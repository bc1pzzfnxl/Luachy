# Database configuration
DB_USER=postgres
DB_PASSWORD=postgrespassword
DB_NAME=luachy

# Workspace Credentials
LUACHY_USER=admin
LUACHY_PASSWORD=password123

# Set to true if hosting behind HTTPS/SSL proxy
SECURE_COOKIES=false

# Backend configuration (used by Docker Compose default)
# DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}
